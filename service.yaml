apiVersion: v1
kind: Service
metadata:
  name: inspireme-service
spec:
  type: NodePort
  selector:
    app: inspireme
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
